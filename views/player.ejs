<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>
      <%= player.first_name %> <%= player.last_name %> - Player Details
    </title>
    <link rel="stylesheet" href="/css/player.css" />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="player-details">
      <h1><%= player.first_name %> <%= player.last_name %></h1>

      <div class="details-grid">
        <p><strong>Country:</strong> <%= player.country %></p>
        <p><strong>World Ranking:</strong> #<%= player.world_ranking %></p>
        <p><strong>Playing Hand:</strong> <%= player.playing_hand %></p>
        <p><strong>Age:</strong> <%= player.age %></p>
      </div>

      <form action="/players/<%= player.id %>/delete" method="POST">
        <button type="submit" class="delete-btn">Delete Player</button>
      </form>

      <a href="/players">Back to Players List</a>
    </div>
  </body>
</html>
