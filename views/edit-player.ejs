<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Edit <%= player.first_name %> <%= player.last_name %></title>
    <link rel="stylesheet" href="/css/create-player.css" />
  </head>
  <body>
    <%- include('partials/navbar') %>
    <div class="form-container">
      <form action="/players/<%= player.id %>/edit" method="POST">
        <h1>Edit Player Details</h1>
        
        <div class="form-group">
          <label for="first_name">First Name:</label>
          <input
            type="text"
            name="first_name"
            id="first_name"
            value="<%= player.first_name %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="last_name">Last Name:</label>
          <input
            type="text"
            name="last_name"
            id="last_name"
            value="<%= player.last_name %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="country">Country:</label>
          <input
            type="text"
            name="country"
            id="country"
            value="<%= player.country %>"
            required
          />
        </div>

        <div class="form-group">
          <label for="world_ranking">World Ranking:</label>
          <input
            type="number"
            name="world_ranking"
            id="world_ranking"
            value="<%= player.world_ranking %>"
            min="1"
            required
          />
        </div>

        <div class="form-group">
          <label for="playing_hand">Playing Hand:</label>
          <select 
            name="playing_hand" 
            id="playing_hand" 
            required
          >
            <option value="right" <%= player.playing_hand === 'right' ? 'selected' : '' %>>Right</option>
            <option value="left" <%= player.playing_hand === 'left' ? 'selected' : '' %>>Left</option>
          </select>
        </div>

        <div class="form-group">
          <label for="age">Age:</label>
          <input
            type="number"
            name="age"
            id="age"
            value="<%= player.age %>"
            min="15"
            max="120"
            required
          />
        </div>

        <button type="submit" class="form-button">Confirm Changes</button>
      </form>
    </div>
  </body>
</html>